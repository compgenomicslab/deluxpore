process {
    executor = 'slurm'

    withLabel: 'fast' { 
        cpus = 2
        memory = '10 GB'
        queue = 'fast'
        time = '2 hours'
        }

    withLabel: 'not_so_fast' { 
        cpus = 10
        memory = '20 GB'
        queue = 'fast'
        time = '2 hours'
        }
}
